#===============================================================================
# Fichier de credentials pour backup_restore_heron.sh
# 
# SÉCURITÉ:
#   - Ce fichier doit avoir les permissions 600: chmod 600 credentials.conf
#   - Ne jamais commiter ce fichier dans un dépôt git
#   - Placer ce fichier dans /etc/heron_backup/credentials.conf
#
# Installation:
#   sudo mkdir -p /etc/heron_backup
#   sudo cp credentials.conf.example /etc/heron_backup/credentials.conf
#   sudo chmod 600 /etc/heron_backup/credentials.conf
#   sudo chown $(whoami) /etc/heron_backup/credentials.conf
#   # Puis éditer le fichier avec vos vraies valeurs
#===============================================================================

#-------------------------------------------------------------------------------
# DESTINATAIRES EMAIL (obligatoire)
#-------------------------------------------------------------------------------

EMAIL_TO="admin@example.com"
EMAIL_FROM="backup@example.com"

#-------------------------------------------------------------------------------
# CONFIGURATION SMTP (pour EMAIL_METHOD="msmtp")
# Utiliser cette méthode pour Gmail, Outlook, ou tout serveur SMTP
#-------------------------------------------------------------------------------

# Gmail exemple:
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="votre.email@gmail.com"
# SMTP_PASSWORD="votre-app-password"    # Utiliser un "App Password" pas le vrai mot de passe!

# Outlook/Office365 exemple:
# SMTP_HOST="smtp.office365.com"
# SMTP_PORT="587"
# SMTP_USER="votre.email@outlook.com"
# SMTP_PASSWORD="votre-mot-de-passe"

# OVH exemple:
# SMTP_HOST="ssl0.ovh.net"
# SMTP_PORT="587"
# SMTP_USER="votre.email@votredomaine.com"
# SMTP_PASSWORD="votre-mot-de-passe"

SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""

#-------------------------------------------------------------------------------
# CONFIGURATION MAILGUN (pour EMAIL_METHOD="curl")
# Alternative si vous utilisez l'API Mailgun
#-------------------------------------------------------------------------------

MAILGUN_API_KEY=""
MAILGUN_DOMAIN=""

#-------------------------------------------------------------------------------
# NOTES
#-------------------------------------------------------------------------------
#
# Pour Gmail, vous devez:
#   1. Activer l'authentification à 2 facteurs sur votre compte
#   2. Créer un "App Password" (mot de passe d'application):
#      - Aller dans: Compte Google > Sécurité > Mots de passe des applications
#      - Créer un nouveau mot de passe pour "Mail"
#      - Utiliser ce mot de passe dans SMTP_PASSWORD
#
# Pour tester l'envoi d'email:
#   echo "Test" | msmtp --debug votre.email@example.com
#
#===============================================================================
